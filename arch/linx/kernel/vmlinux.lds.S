/* SPDX-License-Identifier: GPL-2.0-only */

/*
 * Linx kernel linker script source.
 *
 * Kbuild preprocesses this file (`.lds.S`) into `vmlinux.lds` in the build
 * directory via scripts/Makefile.build. Keep the actual script content in the
 * adjacent `vmlinux.lds` file for now.
 */

#include "vmlinux.lds"

